# Database migrations

Alembic migrations are intentionally empty in Stage 1 because no application
schema exists yet. Future schema changes should add revisions here and use the
async PostgreSQL URL generated by `rag_eval.config.Settings`.
